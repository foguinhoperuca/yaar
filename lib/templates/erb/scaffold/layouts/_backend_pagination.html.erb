<h2><%=t 'general.backend_pagination.title'%> <small><%=t 'general.backend_pagination.explanation' %></small></h2>
<%= will_paginate entities, params: { per_page: @per_page } %>
<%#= page_entries_info(entities, {:model => t('module_name.#{entities}')}) %>
<%= page_entries_info(entities) %>
<strong><%=t 'general.per_page'%></strong>
<%= link_to(10, polymorphic_url([controller_name.classify.constantize], :per_page => 10)) %> |
<%= link_to(25, polymorphic_url([controller_name.classify.constantize], :per_page => 25)) %> |
<%= link_to(50, polymorphic_url([controller_name.classify.constantize], :per_page => 50)) %> |
<%= link_to(100, polymorphic_url([controller_name.classify.constantize], :per_page => 100)) %> |
<%= link_to(200, polymorphic_url([controller_name.classify.constantize], :per_page => 200)) %> |
<%= link_to(500, polymorphic_url([controller_name.classify.constantize], :per_page => 500)) %> |
<%= link_to(1000, polymorphic_url([controller_name.classify.constantize], :per_page => 1000)) %> |
<%= link_to(t('general.link.index'), polymorphic_url([controller_name.classify.constantize], :per_page => "all")) %>
<hr />
<h3><%=t 'general.backend_pagination.table_frontend' %></h3>
